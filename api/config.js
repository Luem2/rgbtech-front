const dotenv = require("dotenv");
dotenv.config();

const {
	DB_USER,
	DB_PASSWORD,
	DB_HOST,
	PORT,
	PGUSER,
	PGPASSWORD,
	PGHOST,
	PGPORT,
	PGDATABASE,
	CLOUDINARY_CLOUD_NAME,
	CLOUDINARY_API_KEY,
	CLOUDINARY_API_SECRET,
	SECRET,
} = process.env;

module.exports = {
	DB_USER,
	DB_PASSWORD,
	DB_HOST,
	PORT,
	PGUSER,
	PGPASSWORD,
	PGHOST,
	PGPORT,
	PGDATABASE,
	CLOUDINARY_CLOUD_NAME,
	CLOUDINARY_API_KEY,
	CLOUDINARY_API_SECRET,
	SECRET,
};
